
	<% @users.each do |user| %>

	 <div id="person-<% user.id %>" class="person">
		
		<img style="display: inline-block" src="<%= user.avatar.url %>" />
	
			<div class="overlay">

				<h2><%= user.name %></h2>

				<p class="tag">I <%=user.missions.first.category.name.downcase %>&nbsp;<%=user.missions.first.statement %> </p>

				<div class='icons'>
					<a href="http://twitter.com/<%= user.url1 %>" class="twitter">Twitter</a>
					<a href="<%= user.url2 %>" class="link">URL</a>
					<a href="<%= user.url3 %>" class="link">URL</a>
				</div>
<!--
				<div class="tags">
					<a href="#">Drupal</a>
					<a href="#">Banjos</a>
					<a href="#">Sandwhiches</a>
				</div>
-->
			</div>
		<!--
			    <td><%= link_to 'Show', user %></td>
			    <td><%= link_to 'Edit', edit_user_path(user) %></td>
			    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
			-->
		</div>
	<% end %>
