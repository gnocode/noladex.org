<%= form_for(@user, :html => { :multipart => true } ) do |f| %>

	<h2>+Add Yourself</h2>

  <% if @user.errors.any? %>

    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
      

</div>
  <% end %>
	
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :placeholder => 'Jane Doe' %>
  </div>
  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email, :placeholder => 'jane@doe.com' %>
  </div>
  <div class="field">
	  <%= f.label :avatar %>
    <%= f.file_field :avatar %>
  </div>
  <!-- <div class="field">
      <%= f.label :admin %>
      <%= f.check_box :admin %>
    </div> -->
  <div class="field">
    <%= f.label :Twitter %>
    <%= f.text_field :url1, :placeholder => "@whatever" %>
  </div>  
  <div class="field">
    <%= f.label :URL %>
    <%= f.text_field :url2, :placeholder => "http://janedoe.com" %>
  </div>      
  <div class="field">
    <%= f.label :URL %>
    <%= f.text_field :url3, :placeholder => "http://johndoe.com" %>
  </div>    

	<div class="field">
		I&nbsp;
		<%= select_tag("categories_1", options_from_collection_for_select(Category.all, "id", "name", params[:categories_1])) %>&nbsp;
		<%=text_field_tag 'mission_statement_1', params[:mission_statement_1] %>
	</div>
	<div class="field">
	  I&nbsp;
	  <%= select_tag("categories_2", options_from_collection_for_select(Category.all, "id", "name", params[:categories_2])) %>&nbsp;
	  <%=text_field_tag 'mission_statement_2', params[:mission_statement_2] %>
	</div>
	<div class="field">
	  I&nbsp;
	  <%= select_tag("categories_3", options_from_collection_for_select(Category.all, "id", "name", params[:categories_3])) %>&nbsp;
	  <%=text_field_tag 'mission_statement_3', params[:mission_statement_3] %>
	</div>
	    
  <div class="actions">
    <input id="user_submit" name="commit" type="submit" value="Submit" class="button">
  </div>

<% end %>